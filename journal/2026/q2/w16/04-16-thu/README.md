# 2026-04-16 (Thursday)

Deep technical exploration of Rust HDLs (RustHDL, RHDL, Kaze, Spade, Veryl), RHDL compiler pipeline (syn→RHIF IR→RTL→Verilog), P4-to-WASM compilation challenges, symbolic execution with Lean, and practical symbolic executor design patterns (flat list output, copy-and-continue forking, CoW optimization).

## Sessions

- **session-00**: Daily log (Gemini) — Rust HDL survey: RustHDL (mature, transpiles to Verilog), RHDL (next-gen, fast simulation, enum payloads), Kaze (lightweight circuit graph → Verilog/Rust), Spade (standalone Rust-inspired HDL), Veryl (Rust-DNA standalone HDL, Cranelift RTL sim), Calyx (IL for HW accelerators, Rust tooling), LLHD (LLVM-for-HW IR, merged into CIRCT). RHDL compiler pipeline: AST extraction via syn crate (proc macros, TokenStream → typed AST), expression transformation (if/match → MUX), lowering to RHIF (SSA-based, strongly typed, formal verification friendly), RTL lowering, Verilog emission. More Rust eDSLs: Clap (ZK circuit constraints), Comet-rs (tensor algebra → MLIR), HIRL (experimental, restrictive RTL semantics). P4-to-WASM compilation gaps: match-action tables vs linear memory (synthesize hash tables/tries or use host imports), FSM parser states vs structured control flow (trampoline loop needed), arbitrary bit-widths vs i32/i64 (bitmask+shift lowering), externs→WASM imports (cleanest mapping). Symbolic execution with Lean: custom SymEx tactics (AST walking, tactic-driven proof), verified SymEx engine (Leanette project), certifying external SMT output (ingest execution tree into Lean for machine-checked proof). Galois SAW uses dependent type theory for LLVM decompilation verification. Symbolic execution tree: root (PC=True), branching on symbolic conditions, leaf nodes (SAT/UNSAT), path explosion challenge. Practical design: flat list of paths (KLEE .ktest files), embarrassingly parallel SMT dispatch, copy-and-continue state cloning (pure isolation), CoW as optimization when memory pressure hits

## Agent index

- RUST-HDL: RustHDL (mature, #[hdl_gen] transpiler), RHDL (next-gen by same author, RHIF SSA IR, 10-100x faster sim, enum payloads), Kaze (lightweight graph→Verilog/Rust), Spade (standalone, Rust-inspired syntax+types), Veryl (standalone, Cargo-like tooling, Cranelift sim). Infrastructure: Calyx (IL for HW accelerators), LLHD (LLVM-for-HW, merged into CIRCT) (s00)
- RHDL-COMPILER-PIPELINE: syn crate parses Rust TokenStream → typed AST → expression transform (if/match→MUX) → RHIF (SSA, strongly typed, formal-verification-friendly) → RTL lowering → Verilog emission. Key: quote crate generates runtime code that builds HW graph, not direct execution (s00)
- P4-TO-WASM: five semantic gaps — (1) match-action tables: no native associative arrays, need software hash/trie or host imports with latency penalty, (2) FSM parser: no goto, need trampoline loop or nested blocks, (3) arbitrary bit-widths: bitmask+shift lowering to i32/i64, (4) externs→WASM imports (clean), (5) verification: translation validation needed. Ref: "Full-Stack Correctness in Wasm" talk (s00)
- LEAN-SYMEX: three approaches — (1) custom tactics walking Lean AST for symbolic execution during proof, (2) verified SymEx engine in Lean (Leanette), (3) certify external SMT output by ingesting execution tree into Lean. Galois SAW precedent (dependent type theory for LLVM verification) (s00)
- SYMEX-PRACTICAL-DESIGN: flat path list output (KLEE .ktest model), not tree. Embarrassingly parallel SMT dispatch. Copy-and-continue forking (pure isolation, simple). CoW optimization path for memory pressure (shared read-only parent, clone-on-write per page). PathGroup for active management, PathSet for output (s00)
