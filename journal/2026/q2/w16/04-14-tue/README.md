# 2026-04-14 (Tuesday)

Wide-ranging technical exploration — C preprocessor alternatives (modules, generics, comptime), symbolic execution naming conventions (PathGroup/PathSet/ExecutionFrontier), protobuf set representation (map vs repeated), comptime feature survey (D/C++/Jai/Rust/Circle), division by zero in type theory (Buzzard FAQ), and ISA instruction fuzzer methodology (coverage-guided feedback, differential testing, semantic generation).

## Sessions

- **session-00**: Daily log (Gemini) — C preprocessor alternatives (modules, generics, comptime, hygienic macros) and Python equivalents, symbolic execution naming conventions (PathGroup/PathSet/ExecutionFrontier), protobuf set representation (map vs repeated), comptime/CTFE survey (D/C++/Zig/Jai/Rust/Circle) and implementation challenges, division by zero in type theory (Buzzard FAQ, total functions in Lean/Rocq), ISA instruction fuzzer methodology (grammar-aware generation, coverage-guided feedback, differential testing, tools: DifuzzRTL/Cascade/RFuzz/ProcessorFuzz/PreSiFuzz/DiveFuzz)
- **session-01**: Journal sync + instruction fuzzing deep dive — ran /sync-journal for Apr 12-14. Searched journal for fuzzing library from sail-xisa work, found proptest (property-based testing, 7 bugs). Identified TestRIG (REMS Cambridge) as the tool user was recalling: RVFI-DII interface, QuickCheck-based generation (Haskell, template DSL, weighted distribution, domain-expert generators, QuickCheck shrinking, ArchDesc conditional generation, NOT coverage-guided). State of art survey (2024-2026): HyPFuzz (hybrid formal+fuzz, USENIX Security 2024), LLM-assisted generation (ChatFuzz, LLM4Fuzz), microarchitectural state abstraction, MorFuzz (morphing-based mutation), Revizor (Microsoft, real silicon, HPCs), SIGFuzz (taint+speculation), SpecDoctor. Industry: Intel/ARM/Google OpenTitan/RISC-V Sail+Spike. Trend: hybrid formal+fuzz with microarchitectural coverage and security-oriented oracles: modules (C++20 import, Rust mod/use), typed constants (constexpr/const), generics/inline (templates, traits), conditional compilation (Zig comptime, C++17 if constexpr, Rust #[cfg]), hygienic macros (Rust macro_rules!/proc macros, Swift macros). Python equivalents: import system, typing.Final, decorators, load-time if, metaclasses + ast module. Symbolic execution naming: SimulationManager (angr), ExecutionTree, Executor/Searcher (KLEE), ExecutionState/SimState/SymbolicState for individual paths. Collection naming: ExecutionFrontier (active search edges), PathSet/TraceSet (mathematical output), PathGroup (management container, angr historical). PathList leaks implementation, PathSet for formal output, PathGroup for active management. Protobuf set: map<T, bool> (uniqueness via key, O(1) lookup, wire overhead) vs repeated (compact, app-level dedup). Comptime survey: D (2007 pioneer, automatic CTFE), C++ (constexpr→consteval evolution), Jai (#run directive), Rust (const fn, restricted), Circle (@meta). Implementation challenges: compiler becomes VM, cross-compilation trap (host vs target), state/memory/IO management, types↔values blur (dependent type theory territory). Division by zero in type theory: Buzzard FAQ on total functions in Lean/Rocq, 1/0=0 convention ("alien field"), burden shifts from syntax to theorem application. ISA instruction fuzzers: differential testing (golden model vs DUT), coverage-guided feedback. Tools: ProcessorFuzz (CSR-transition coverage), PreSiFuzz (Intel, LibAFL), DifuzzRTL (register-coverage), Cascade (ETH Zurich, intricate program generation, CVEs found), RFuzz (UC Berkeley, Chisel/FIRRTL + Verilator + Rust), DiveFuzz (XiangShan). Methodology: grammar-aware generation (valid opcodes), HW-specific coverage metrics (toggle, FSM, CSR), intentional hazard generation (RAW/WAW/WAR data hazards, control hazards, memory hazards), differential testing as oracle

## Agent index

- C-PREPROCESSOR-ALTERNATIVES: five replacement categories — modules (C++20/Rust/Go/Zig), typed constants (constexpr/const), generics+inline, conditional compilation (comptime/if constexpr/#[cfg]), hygienic macros (Rust proc macros, Swift). Python: import, typing.Final, decorators, load-time if, metaclasses+ast (s00)
- COMPTIME-SURVEY: D (2007 pioneer, automatic CTFE), C++ (constexpr C++11 → consteval C++20), Zig (flagship), Jai (#run, unreleased), Rust (const fn, restricted), Circle (@meta experimental). Implementation hard: compiler=VM, cross-compilation trap (host vs target sizeof), memory management, dependent-type-theory territory (s00)
- SYMBOLIC-EXECUTION-NAMING: SimulationManager (angr, stash-based), ExecutionTree/StateGraph (tree/DAG), Executor/Searcher (KLEE). Collections: PathGroup (active management, abstract), PathSet (formal output, unique PCs), ExecutionFrontier (search edges). PathList leaks implementation detail. Individual: ExecutionState/SimState/SymbolicState (s00)
- PROTOBUF-SET: map<T,bool> for uniqueness+O(1) lookup (wire overhead), repeated for compact wire format (app-level dedup). No native set type (s00)
- DIVISION-BY-ZERO-TYPE-THEORY: Buzzard FAQ — total functions in Lean/Rocq require 1/0=0 ("alien field"). Burden shifts from syntax evaluation to theorem invocation (must prove a!=0 to use a/a=1). Pragmatic tradeoff: CS prefers total functions for automation, mathematicians find it offensive (s00)
- ISA-FUZZERS: differential testing (golden model vs DUT). Key methodology: grammar-aware generation (valid opcodes bypass decoder), coverage-guided feedback (toggle/FSM/CSR metrics), intentional hazard generation (data/control/memory hazards). Tools: DifuzzRTL (register-coverage), Cascade (ETH Zurich, CVEs), RFuzz (Berkeley, Chisel+Verilator+Rust), ProcessorFuzz (CSR-transition), PreSiFuzz (Intel, LibAFL), DiveFuzz (XiangShan). Applicable to XISA simulator as golden model (s00, s01)
- TESTRIG: REMS group Cambridge, QuickCheck-based RISC-V differential testing via RVFI-DII. Grammar-aware biased random generation (Haskell QuickCheckVEngine), template DSL with weighted distribution, domain-expert generators, QuickCheck shrinking for minimal counterexamples, ArchDesc conditional generation. NOT coverage-guided or SMT-based (s01)
- ISA-FUZZ-STATE-OF-ART-2024-2026: HyPFuzz (hybrid formal+fuzz, USENIX Security 2024), LLM-assisted (ChatFuzz, LLM4Fuzz — emerging), MorFuzz (constraint-preserving morphing), Revizor (Microsoft, real silicon HPCs, leakage detection), SIGFuzz (taint+speculation), SpecDoctor (microarchitectural contracts). Trend: hybrid formal+fuzz with security-oriented oracles (s01)
- PROPTEST: Rust property-based testing, used in sail-xisa differential testing, found 7 bugs (s01)
