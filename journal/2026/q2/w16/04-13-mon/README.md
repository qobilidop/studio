# 2026-04-13 (Monday)

Hardware formal specification deep dive — Lean for HW, state of art survey (Sail, FormalRTL, Jasper/HECTOR), C++-to-RTL equivalence checking (algorithmic subset, bit-blasting, SAT/SMT), open-source tooling (ac_types, HW-CBMC, CIRCT, Bambu, Dynamatic), ac_types ecosystem (MatchLib, Intel oneAPI, CERN da4ml), and Webb's Theorem / functional completeness in universal algebra.

## Sessions

- **session-00**: Daily log (Gemini) — Lean for HW formal specification (eDSLs with dependent types, SMT integration for bit-vector logic, "time-warping" speculative simulation, verified compilers/optimizers). State of art in HW formal spec: eDSLs in host languages, deep SMT integration during construction, verified code generation from IRs, HW-SW co-verification. Real projects: Sail (ISA spec gold standard, RISC-V/Arm official), Lean FRO + community HDLs ("Sparkle"), FormalRTL (arXiv:2603.08738, C/C++ reference models + hw-cbmc), commercial tools (Jasper/HECTOR). C++-to-RTL formal equivalence: constrained "algorithmic subset" C++ (no dynamic memory, no unbounded loops, bit-accurate ac_int types), bit-blasting both C++ DAG and RTL into boolean logic, SAT/SMT solver proves equivalence (UNSAT = proven). RTL X-state handling via dual-rail encoding. Open-source tooling: ac_types (HLSLibs, Apache 2.0), NVIDIA MatchLib, HW-CBMC (C-to-RTL equivalence), CIRCT/circt-lec, Bambu HLS, Dynamatic (MLIR-based, dynamically scheduled). ac_types ecosystem: NVIDIA MatchLib (DNN accelerator tapeouts), Intel oneAPI DPC++ for FPGAs, CERN da4ml (LHC trigger inference), HLSRewriter (LLM guardrail via ac_int compile-time checks). User considering ac_int for Z3Wire. Webb's Theorem (1935, N-valued logic functional completeness), Sheffer stroke (NAND/NOR), Sierpinski's theorem on clones (infinite sets), new Odrzywołek paper on eml(x,y) = exp(x) - ln(y) as continuous Sheffer stroke

## Agent index

- HW-FORMAL-SPEC: state of art = correct-by-construction via eDSLs in host languages (C++/Kotlin/Python/Lean 4/Rocq), deep SMT during construction (not just backend), verified codegen from IR, HW-SW co-verification (AXI-Lite, PCIe, DMA). Real projects: Sail (RISC-V/Arm official ISA spec), FormalRTL (arXiv:2603.08738), Jasper/HECTOR commercial (s00)
- C-TO-RTL-EQUIVALENCE: "algorithmic subset" C++ (no malloc, no unbounded loops, ac_int bit-accurate types) → bit-blast to boolean DAG → SAT/SMT proves equivalence with RTL. Dual-rail encoding for X-states. Used in HLS sign-off (Siemens SLEC). NVIDIA tensor cores, crypto verified this way. Open-source: HW-CBMC (SSA + SAT/SMT), CIRCT circt-lec (s00)
- AC_TYPES: HLSLibs open-source (Apache 2.0), ac_int<W>/ac_fixed<W,I>. Natural growth arithmetic (ac_int<4> + ac_int<5> → ac_int<6>). Ecosystem: NVIDIA MatchLib (DNN tapeouts), Intel oneAPI FPGAs, CERN da4ml (LHC triggers), HLSRewriter (LLM guardrail). User considering ac_int for Z3Wire — compile-time bit-width inference then SMT for logic (s00)
- HLS-OPEN-SOURCE: Bambu/PandA (C/C++ → Verilog/VHDL, GCC/Clang frontend), Dynamatic (EPFL, MLIR-based, dynamically scheduled circuits for irregular memory/control). Both take algorithmic subset C++ (s00)
- LEAN-HW: Lean FRO lists HW verification as mandate. Dependent types for bit-width control, SMT tactics (grind) for bit-vector goals, "Sparkle" community HDL, time-warping simulation (proof-backed cycle skipping, >100M cycles/sec, beats Verilator in some scenarios) (s00)
- WEBBS-THEOREM: Webb (1935) generalized Sheffer stroke to N-valued logic — single binary operator generates all truth functions. Sheffer (1913, NAND/NOR for Boolean). Sierpinski (1945, infinite sets). New: Odrzywołek's eml(x,y) = exp(x) - ln(y) as continuous functional completeness (HN discussion item#47746610) (s00)
