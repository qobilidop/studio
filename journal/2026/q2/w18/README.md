# 2026 W18 (Apr 27–May 3)

A largely off-week dominated by a busy work period (new responsibilities landed Friday) — only Mon delivered substantive work: a deep RDMA formal-verification + RDMA-specific DSL exploration plus a focused PR submitting P4PER-4 Draft to p4lang/p4per (with extensive Zensical/devcontainer gotchas captured). Mid-week was emotional/check-in only; Sat covered Pyret pedagogy; Sun reflected on weekend chore-domination with a Friday-night-intent practice suggestion.

## Days

- [04-27-mon](04-27-mon/README.md): RDMA formal verification deep dive ($\text{RDMA}^{\text{TSO}}$ semantics, SHIFT Failover Trilemma in Rocq, RNIC hardware verification via BMC/SMT, refinement RTL→Verbs ISA, RDMA-specific DSLs JANUS/RDMI/DOCA-DPL/Derecho); P4PER-4 Draft PR #7 submitted to p4lang/p4per with Zensical 0.0.36 plugin reality (`awesome-nav` not implemented), explicit `nav` workaround, devcontainer gotchas, Editor/Approver role-split convention
- [04-28-tue](04-28-tue/README.md): Iterative DFS that maintains stack = root-to-current-node path — binary tree (post-order with `prev` pointer) and N-ary tree (`(node, child_index)` tuples)
- [04-29-wed](04-29-wed/README.md): Brief check-in — work busy, feeling sad, no time for personal projects
- [04-30-thu](04-30-thu/README.md): Brief check-in — feeling better, hoping next milestone clears time for Z3Wire/p4py/Lean/Rocq/Haraway
- [05-01-fri](05-01-fri/README.md): Brief check-in — unexpected new work responsibilities landed, life getting busier but it's something user cares about
- [05-02-sat](05-02-sat/README.md): Pyret programming language overview (Brown PLT pedagogical, `where`-block TDD, ADTs+`cases`, web-first → JS, gradual typing with predicate annotations)
- [05-03-sun](05-03-sun/README.md): Reflective Chinese check-in — full day of housework, recent weekends feeling unproductive. Gemini suggested Friday-night weekend-intent practice + batch-process chores + "micro-flow" weekend work

## Agent index

- BUSY-PERIOD-W18: dominant theme of the week. New office settling-in, heavy day-job cognitive load, then unexpected new work responsibilities landed Friday. Emotional arc: Wed sad → Thu better-but-still-constrained → Fri positive-framing of new scope. Personal-project bandwidth essentially zero this week. Pattern continues from W17 "very busy" notes
- WEEKEND-CHORE-DOMINATION: Sat partially used (Pyret survey), Sun fully consumed by housework. User identified as a problem to fix. Gemini proposed Friday-night weekend-intent practice + chore batching + micro-flow over high-intensity engineering. User has not yet committed
- RDMA-FORMAL-VERIFICATION (Mon): $\text{RDMA}^{\text{TSO}}$ memory semantics formalize RDMA over x86 TSO; SHIFT mechanized "RDMA Failover Trilemma" in Rocq (~3500 LOC) — exactly-once + receiver-NIC opacity + zero-copy is impossible to simultaneously guarantee. Target-agnostic transport eDSLs reduce state-space explosion vs raw `rdma-core` C
- RNIC-HARDWARE-VERIFICATION (Mon): BMC + SMT (Z3) on RTL blocks (DMA engine, WQE cache controller). RNIC = multi-core distributed system between PCIe + Ethernet, manages 100k+ Queue Pairs. Refinement proofs RTL → Verbs/RC transport state machine via Rocq/Lean 4. P4-inspired data-plane eDSLs auto-generate RoCEv2 parser hardware + verification models
- RDMA-SPECIFIC-DSLS (Mon): JANUS = ACL policy DSL with QP semantics; RDMI = memory introspection policy DSL → P4 switches; NVIDIA DOCA Pipeline Language (DPL) = P4-16 derived for BlueField DPUs (NVMe-oF/iSER offload); Derecho = C++ embedded epistemic logic for RDMA atomic multicast. Concrete DSL examples user wanted after pushing back on past-context overfitting
- P4PER-4-DRAFT (Mon): PR #7 to p4lang/p4per (`p4per-4` branch). Defines P4PER mechanism modeled on PEP/EIP — types Technical/Informational/Process, roles Author/Champion/Editor/Approver (Approver≠author), 5-step lifecycle (Reserve → Submit → Refine → Decide → Mark complete) + Withdrawal only-from-Draft. Role-split: Editor merges content/admin/Withdrawn, Approver merges decisions
- ZENSICAL-PLUGIN-REALITY (Mon): Zensical 0.0.36 advertises MkDocs plugin compat as roadmap but only hardcodes `search/offline/mike/mkdocstrings/autorefs/glightbox` (per `python/zensical/config.py:_convert_plugins`). `awesome-nav` is NOT implemented — installing pip plugin + dropping `.nav.yml` does nothing. Workaround for natural-number nav: explicit `nav = [{"Title" = "path.md"}, ...]` in `[project]`. Trust-but-verify: Claude was corrected on its initial wrong claim
- P4PER-DEVCONTAINER-GOTCHAS (Mon): `./dev.sh` silences `devcontainer up` stderr (silent failures); port 8000 conflicts have multiple causes (host SSH forward / colima mux / stale `zensical serve` in container); `colima docker socket` goes stale (restart kills containers); `zensical serve` does not watch `zensical.toml`; container path `/workspaces/p4lang__p4per` (double underscore)
- DFS-PATH-STACK (Tue): how to write iterative DFS where stack equals root-to-current path. Binary tree: post-order with `prev` pointer, only pop when both subtrees done. N-ary tree: stack stores `(node, child_index)` tuples; on pop, re-push parent with `idx+1` then push child with `0`. Use cases: LCA, path-sum
- PYRET-LANG (Sat): Brown PLT pedagogical lang (HtDP lineage). `where`-block TDD attached to functions. Python-like syntax + explicit `end`. Native ADTs + `cases`. Browser-only via code.pyret.org → JS. Gradual typing + predicate annotations (e.g., positive Number, dynamic). Bootstrap curriculum middle/high school
- USER-PREFS-AGENTIC (Mon, s01): minimal Draft (push back on non-load-bearing additions), terse responses, trust-but-verify external tool claims, concrete diff suggestions over abstract feedback, ignore /loop TaskCreate reminders
- AGENT-FEEDBACK-OVERFITTING (Mon, s00): Gemini overfit on past Z3Wire/Rocq context when asked about RDMA eDSLs; user correction: "don't overfit on our past conversations." Lesson: when user asks domain-specific question, answer in domain even if past adjacent context tempts otherwise
