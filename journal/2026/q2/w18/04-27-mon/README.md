# 2026-04-27 (Monday)

RDMA formal verification deep dive (transport semantics, RNIC hardware verification, RDMA-specific DSLs like JANUS/RDMI/DOCA-DPL/Derecho), then a focused work session submitting P4PER-4 Draft PR to p4lang/p4per with extensive zensical/devcontainer setup gotchas captured.

## Sessions

- **session-00**: Daily log (Gemini). RDMA + formal verification exploration. (1) Transport-layer RDMA verification: $\text{RDMA}^{\text{TSO}}$ memory semantics (RDMA over x86 TSO), mechanized fault-tolerance proofs (SHIFT in Rocq, "RDMA Failover Trilemma" — exactly-once vs receiver-NIC opacity vs zero-copy is impossible), target-agnostic transport eDSLs. (2) RDMA hardware verification: BMC/SMT (Z3) on RNIC RTL blocks (DMA engine, WQE cache), high-level refinement proofs RTL→Verbs ISA via Rocq/Lean 4, P4-inspired data-plane eDSLs for RoCEv2 parsers. (3) eDSL examples broadly: Chisel (Scala), Hardcaml (OCaml/Jane Street), P4 in Kotlin, Rosette (Racket symbolic execution), C++ SMT abstractions (`z3w::` namespace tax pattern). (4) RDMA-specific DSLs after pushback: JANUS ACL policy DSL with QP semantics, RDMI memory-introspection DSL → P4 switches, NVIDIA DOCA Pipeline Language (DPL, P4-16 derived for BlueField DPUs, NVMe-oF/iSER offload), Derecho (C++ embedded epistemic logic for RDMA atomic multicast). User pushed back: "don't overfit on our past conversations" — Gemini was leaning on Z3Wire/Rocq context too heavily
- **session-01**: P4PER-4 Draft + zensical nav (Claude Code). Submitted PR https://github.com/p4lang/p4per/pull/7 — P4PER-4 "P4PER Purpose and Guidelines" Draft. Repo: p4lang/p4per branch `p4per-4`. Devcontainer Ubuntu 24.04 + `uv tool install zensical`. Captured recurring gotchas: (1) `./dev.sh` redirects `devcontainer up` stderr to /dev/null — silent failures; (2) port 8000 conflicts (host SSH forward / colima mux / stale `zensical serve` in container); (3) `colima docker socket` goes stale, `colima restart` kills containers; (4) `zensical serve` watches `docs/` but not `zensical.toml`. Zensical findings: default nav is lexicographic, explicit `nav = [...]` in `[project]` is the only working override; **`awesome-nav` plugin NOT implemented in Zensical 0.0.36** despite roadmap claim — verified by reading `python/zensical/config.py:_convert_plugins` (only `search/offline/mike/mkdocstrings/autorefs/glightbox` actually wired). P4PER-4 doc structure: types Technical/Informational/Process, roles Author/Champion/Editor/Approver (Approver≠author), 5-step lifecycle (Reserve→Submit→Refine→Decide→Mark complete) + Withdrawal only-from-Draft. Role-split: Editor merges content/admin/Withdrawn PRs, Approver merges decision PRs (Accepted/Active/Rejected/Final). Known deferred gaps (Active→Final transition not in lifecycle steps; status bullet order mismatches diagram — user declined reorder; bare `P4PER-N` placeholder; loose "appointed by P4 TST members or P4 WG chairs"). User preferences observed: minimal Draft, terse responses, trust-but-verify external claims, concrete diff suggestions over abstract feedback, ignore TaskCreate /loop reminders

## Agent index

- RDMA-FORMAL-VERIFICATION: $\text{RDMA}^{\text{TSO}}$ memory semantics formalize RDMA atop x86 TSO; SHIFT project mechanized "RDMA Failover Trilemma" in Rocq (~3500 LOC) — cross-NIC failover cannot simultaneously guarantee exactly-once + receiver-NIC opacity + zero-copy. Target-agnostic transport eDSLs reduce state-space explosion vs raw `rdma-core` C (s00)
- RNIC-HARDWARE-VERIFICATION: BMC + SMT (Z3) at RTL for DMA engine / WQE cache controller. RNIC = multi-core distributed system between PCIe + Ethernet, manages 100k+ Queue Pairs, caches WQEs, maintains Page Translation Tables. Refinement proofs RTL → Verbs/RC transport state machine via Rocq/Lean 4. P4-inspired eDSLs auto-generate RoCEv2 parser hardware + verification models (s00)
- RDMA-SPECIFIC-DSLS: JANUS = ACL policy DSL with Queue Pair semantics (line-rate kernel-bypass-preserving); RDMI = memory introspection policy DSL compiled to P4 switches (network-fabric-resident security monitoring bypassing host blindness); NVIDIA DOCA Pipeline Language (DPL) = P4-16 derived for BlueField DPUs, NVMe-oF/iSER offload; Derecho = C++ embedded epistemic logic for RDMA atomic multicast / fault-tolerant coordination (s00)
- EDSL-EXAMPLES-BROADER: Chisel (Scala HW eDSL, RISC-V ecosystem), Hardcaml (OCaml/Jane Street), P4 in Kotlin (advanced types + build tools), Rosette (Racket symbolic execution → SMT), C++ SMT abstractions (aggressive namespace minimization e.g. `z3w::` to manage formula readability) (s00)
- AGENT-FEEDBACK-OVERFITTING: Gemini leaned too heavily on past Z3Wire/Rocq context when answering RDMA eDSL question. User correction: "I mean specifically for RDMA. Also don't overfit on our past conversations." Lesson: when user asks domain-specific question, answer in domain even if past context suggests adjacent themes (s00)
- P4PER-4-DRAFT-PR: PR #7 submitted to p4lang/p4per (`p4per-4` branch). P4PER = P4 Enhancement Proposal mechanism modeled on PEP/EIP. Defines types (Technical/Informational/Process), roles (Author/Champion/Editor/Approver, Approver must differ from authors), 5-step lifecycle (Reserve number → Submit Draft → Refine → Decide → Mark complete + Withdraw only-from-Draft). Role-split: Editor merges content/admin/Withdrawn, Approver merges decisions (Accepted/Active/Rejected/Final). Eligibility: TST members, P4 WG chairs, anyone TST/WG-chair-appointed (s01)
- ZENSICAL-PLUGIN-REALITY: Zensical 0.0.36 advertises MkDocs plugin compatibility roadmap but only hardcodes a small subset (`search/offline/mike/mkdocstrings/autorefs/glightbox` per `python/zensical/config.py:_convert_plugins`). `awesome-nav` is NOT implemented — installing the pip plugin + dropping `.nav.yml` does nothing. For natural-number nav order today, explicit `nav = [{"Title" = "path.md"}, ...]` in `[project]` is the only working option. Zero-padding filenames rejected (URL churn) (s01)
- P4PER-DEVCONTAINER-GOTCHAS: (1) `./dev.sh` redirects `devcontainer up` stderr to /dev/null — silent failures; run `devcontainer up --workspace-folder .` directly to debug. (2) Port 8000 conflicts have multiple causes: host SSH forward, colima SSH mux (leave it), stale `zensical serve` in container (`docker exec <name> sh -c 'pkill -f "zensical serve"'`). (3) Colima docker socket goes stale; `colima restart` kills containers. (4) `zensical serve` watches `docs/` but NOT `zensical.toml` — config changes need manual restart. (5) Container path is `/workspaces/p4lang__p4per` (double underscore) (s01)
- USER-PREFS-P4LANG: minimal Draft (push back on non-load-bearing additions), terse responses, trust-but-verify external tool claims (was corrected on Zensical plugin support), concrete diff suggestions over abstract feedback, ignore /loop TaskCreate reminders (s01)
