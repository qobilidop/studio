# 2026-04-10 (Friday)

MoonBit formal verification discussion (built-in proof primitives, comparison with Dafny/Ada/Eiffel/D), AI data modality diagram analysis (Moravec's paradox, blessed vs cursed zones), and blogging stack evaluation (Zola vs Hugo vs Astro, Markdown extensions, shortcodes, frontmatter in MkDocs/Zensical).

## Sessions

- **session-00**: Daily log (Gemini) — MoonBit's `proof_ensure` keyword and `moon prove` CLI for native formal verification, survey of languages with built-in verification primitives (Dafny/Ada SPARK/Eiffel/D), key distinction: compiler+prover sharing semantics vs external annotation tools. AI capabilities diagram (data availability vs abstraction level): NLP/math "blessed" (pre-abstracted tokens), vision/physics "cursed" (continuous→discrete compression hard), robotics double-cursed (low data + low abstraction). Blogging: Zola (Rust, Tera, strict `@/` link checking) vs Hugo (Go Templates, massive theme ecosystem), both extend CommonMark with shortcodes/syntax highlighting/math. MkDocs/Zensical frontmatter for metadata (title, description, tags, theme overrides) but not URL routing. Kubernetes KEPs system (Go/Bash validation + Hugo via kubernetes.dev), enhancement proposal ecosystem survey (PEPs, RFCs, KEEPs, JEPs, EIPs). Documentation systems: mdBook (Rust RFCs), Jekyll (EIPs), GitHub-native (KEEP/React/Vue RFCs), xml2rfc (IETF), Confluence (Kafka KIPs)

## Agent index

- FORMAL-VERIFICATION-PRIMITIVES: MoonBit `proof_ensure` + `moon prove` (2026 release). Club of built-in verification: Dafny (requires/ensures/decreases/invariant, Z3-backed, compiles to C#/Java/Go/Python/C++, used by AWS), Ada 2012 (Pre/Post/Type_Invariant aspects, SPARK subset for static verification), Eiffel (invented Design by Contract 1980s, require/ensure/invariant, AutoProof for static), D (in/out contract blocks, runtime only). Key insight: compiler+prover sharing semantics (Dafny, SPARK, MoonBit) vs external annotation tools (JML for Java, ACSL for C, Liquid Haskell) (s00)
- MORAVEC-PARADOX-DIAGRAM: 2D plot: data availability × abstraction level. NLP+math = "blessed" (high data, high abstraction, pre-compressed symbols). Vision/physics = high data but low abstraction (representation learning hard). Robotics/neuro = double-cursed (low data + low abstraction). Research metadata = low data + high abstraction. Ref: Ziming Liu blog post "everything-is-language" (s00)
- BLOGGING-SSG: Zola (Rust, single binary, Tera/Jinja2 templating, pulldown-cmark, strict @/ internal link checking, syntect with custom .sublime-syntax support) vs Hugo (Go, single binary, Go Templates, Goldmark, Chroma highlighter, massive theme ecosystem, external data fetching). Both extend CommonMark with shortcodes, syntax highlighting, math (KaTeX/MathJax). Deployment: GitHub Action builds static assets (s00)
- MKDOCS-FRONTMATTER: YAML frontmatter supported for title, description, tags, hide (toc/nav), theme overrides. Cannot set URL/permalink — routing strictly filesystem-based. Zensical inherits same behavior (s00)
- ENHANCEMENT-PROPOSALS: PEPs (Python), KEEP (Kotlin), C++ WG21, Rust RFCs, Swift Evolution, JEPs (Java), Go Proposals, KEPs (Kubernetes), KIPs (Kafka), React/Vue RFCs, IETF RFCs, EIPs/BIPs. Doc systems: mdBook, Jekyll, GitHub-native, xml2rfc, Confluence (s00)
